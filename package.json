{
  "dependencies": {
    "classnames": "2.3.1"
  },
  "devDependencies": {
    "esbuild": "0.14.49",
    "esbuild-loader": "2.19.0",
    "webpack": "5.73.0",
    "webpack-cli": "4.10.0"
  },
  "scripts": {
    "build": "webpack build --mode=production",
    "esbuild": "esbuild index.js --minify --bundle --sourcemap",
    "esbuild:linked": "npm run esbuild -- --legal-comments=linked --outfile=dist/linked.js",
    "esbuild:eof": "npm run esbuild -- --legal-comments=eof --outfile=dist/eof.js"
  }
}
